services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pdf_chat_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/code/data
      - ./logs:/code/logs
    restart: always
